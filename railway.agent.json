{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "Dockerfile.agent" },
  "deploy": { "restartPolicyType": "ALWAYS", "numReplicas": 1 }
}
