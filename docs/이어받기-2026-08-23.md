# 이어받기 메모 — 2026-08-23 03:00 KST

`/clear` 이후 이 파일부터 읽는다. 대화에만 있고 파일에 없던 것들을 여기 남긴다.

## 지금 상황 한 줄 (2026-08-23 07:40 갱신)

**"알리미"에서 "처리하는 에이전트"로 바뀌었다.** 어르신이 찍은 우편물을 보호자가 폰에서 **[납부 처리 승인]** 하면
노트북의 워커가 실제 브라우저를 열어 **Solar Pro 4가 페이지 구조를 읽고 행동을 골라** 조회→금액 대조→납부까지 가고,
인증·비밀번호 단계는 **보호자가 폰의 실시간 화면을 직접 눌러** 넘긴 뒤 이어서 끝낸다. 어르신 음성은 ElevenLabs(Yohan Koo).
코드·DB·배포 다 살아 있다. **남은 건 영상·발표자료·Description·실기기 확인.** 시험 289개.

## 마감 (오늘)

| 시각 | 일정 |
|---|---|
| **12:00** | **제출 마감** — 11:00에 제출을 시작한다 |
| 13:00~16:00 | 발표 — 팀당 **5분 + Q&A 2분**, 24팀, **청중이 Upstage 팀** |

제출 폼(`hackjunction.app/participate/44/project`)에 채울 것: **Demo URL · Source Code · Video · Presentation**. Description은 이미 제출됐고 좁혀서 다시 써야 한다.

## 확보된 제출물

| 항목 | 값 |
|---|---|
| **Demo URL** | **https://docdo.vercel.app** (공개 확인 — 인증 벽 없음) |
| **Source Code** | **https://github.com/needsbuilder/docdo** (public, 비로그인 clone 검증 완료) |
| Vercel | 팀 `needslab` / 프로젝트 `docdo` · 별칭 `docdo-needslab.vercel.app` |
| 환경변수 | `UPSTAGE_API_KEY`·`UPSTAGE_AGENT_ID`·`SUPABASE_URL`·`SUPABASE_ANON_KEY` → production·preview·development |
| Supabase | 프로젝트 `docdo` ref `tsucihkkdxqzrplanpki` (ap-northeast-2) · `documents` 테이블 적용 완료 · DB 비밀번호는 `.env` |
| **보호자 계정** | 이메일+비밀번호 가입(`/guardian`). 세션 서명 키 `AUTH_SECRET` 은 `.env`·Vercel 3개 환경. **시연용 계정을 미리 만들고, 어르신 폰에서 초대 링크를 한 번 열어 둔다** |

## 다음에 할 일

**구현 완료.** 남은 것은 전부 사람 손: 실기기 확인 → 영상 → 발표자료 → Description → 11:00 제출.
**반영은 두 번**: 앱 `vercel --prod --yes` · 워커 `railway up --service docdo-agent --detach`. 푸시만으론 둘 다 안 바뀐다.
폰이 옛 화면을 보이면 **탭을 닫고 새로 열기**(09:00에 실제로 겪음 — 머리띠·승인 버튼이 안 보였다).

### 에이전트 실행(07:40) — 승인하면 독도가 납부한다

흐름: 보호자 카드 **[납부 처리 승인 · 73,000원]** → `action_status=queued` → 노트북 워커가 집음 → 실제 Chromium →
Solar Pro 4가 접근성 트리(버튼·입력·링크 글자 + ref 번호)를 읽고 `click/type/press/scroll/wait_human/done/abort` 중 하나씩 →
단계마다 시각·설명·화면(JPEG)이 카드 "독도가 한 일"에 실시간으로 + 0.7초 간격 **실시간 화면** → 인증 화면이면
`waiting`("보호자 차례") → 보호자가 폰에서 실시간 화면을 **눌러 직접 조작**(터치 좌표·글자·Enter 중계) → [이어서 하기] →
납부 완료 → 어르신 화면 "처리됐어요 — 건강보험료 73,000원 납부 완료 · 납부확인번호 …".

**워커는 Railway 에 상시 기동 중(08:50~)** — 노트북 없이 어느 폰에서나 된다.
Railway 프로젝트 `docdo-agent`(0aa9fd23…) · 서비스 `docdo-agent` · `Dockerfile.agent`(Playwright 공식 이미지) ·
변수 `AGENT_SECRET`·`UPSTAGE_API_KEY`·`DOCDO_URL`·`AGENT_MODE=llm`·`AGENT_ADAPTER=demo`·`RAILWAY_DOCKERFILE_PATH`.
재배포: `railway up --service docdo-agent --detach` · 로그: `railway logs --service docdo-agent`(스트리밍이라 타임아웃 주의) · 상태: `railway deployment list --service docdo-agent`.
배포본 E2E 통과(09:03): 폰 승인 → Railway 워커가 집어 조회·대조·납부 클릭 → 보호자 차례 → 원격 터치 → 납부 완료.
**노트북 워커는 띄우지 않는다**(두 워커가 같은 건을 집으면 리스가 한쪽을 끊지만 정리 차원). 프로젝터에 브라우저를 보이고 싶을 때만 Railway 서비스를 잠시 끄고 아래를 쓴다.

**시연 실행 (노트북, 프로젝터에 브라우저가 보인다 — 선택)**
```
DOCDO_URL=https://docdo.vercel.app npm run agent:demo        # Solar 루프 · 브라우저 보임(slowMo)
AGENT_MODE=script   …                                        # 고정 절차 폴백(Solar 장애 시)
AGENT_ADAPTER=giro  …                                        # 인터넷지로 실사이트 — 첫 화면부터 인증 문구 → 바로 보호자 차례
```
- 워커는 `.env`의 `AGENT_SECRET`·`UPSTAGE_API_KEY`를 읽는다. 서버(Vercel)에도 같은 `AGENT_SECRET`이 있다.
- **실행 대상은 앱 안의 시연용 포털 `/demo/giro`** ("시연용 · 실제 납부 아님" 상시 표기). 원장(`lib/demoBills.ts`)이 아는 전자납부번호:
  건보료 `1102-1234-5678-9012`(73,000) · `1120-6032-4087-13912`(32,000) · KT `8801-2233-4455`·`0801-2233-4455-0000`(45,100).
  **시연은 fixtures 01(건보료) 또는 06(KT 통신요금) 사진으로.** 전기·상수도·자동차세는 Extract 가 전자납부번호를 못 읽어(`low`/없음) 버튼이 안 뜬다 — 설정으로 못 바꾼다(원칙 2).
  실제 인터넷지로는 **비회원도 금융인증서·YESKEY 공동인증서 필수**라 조회조차 인증 뒤에 있다 — 인증 이후 단계는 미구현.
- **정확한 표현**: "Solar 컴퓨터 유즈" ✗ (Solar는 이미지를 못 받는다) → ✅ **"Solar가 페이지 구조를 읽고 행동을 결정하는 브라우저 에이전트"**.
  픽셀 computer-use(OpenAI/Anthropic)와 다르다. Upstage 모델 목록에 비전·computer-use 계열 없음(07:25 확인).
- 가드레일은 코드(`scripts/agent-worker.ts`): 인증·비밀번호 문구 보이면 모델 안 부르고 사람에게 · `password` 입력 금지 ·
  납부/결제/이체 버튼은 화면에 문서 금액이 보일 때만 · 허용 도메인 밖 차단 · 같은 화면 3회·25단계 초과 중단 · `done`은 납부 클릭 후만.
- 승인 자체가 막히는 문서: `mismatch`(409) · 전자납부번호/금액이 Extract `high`가 아닌 것(409, 버튼도 안 뜸).
  `unknown_issuer`·`not_checkable`는 **허용**(09:20 완화) — 납부는 문서 계좌가 아니라 포털이 번호로 조회한 고지에 하므로 진짜 가드는 워커의 "포털 금액 = 문서 금액".
- 원격 조작 입력은 서버 큐를 지나지만 저장하지 않고 끝나면 비운다 — 발표에서 명시.
- 어느 폰에서나 쓰려면 워커가 항상 켜진 곳(VPS·Vercel Sandbox·Browserbase)에 있어야 한다. 큐 구조라 `DOCDO_URL`만 바꾸면 된다.
- 재승인은 `none/failed/blocked`에서만. `waiting`에 박힌 시험 문서는 DB에서 `action_status='failed'`로 풀었다(psql, 풀러 `aws-0-ap-northeast-2.pooler.supabase.com:5432`).
- DB 컬럼 추가됨(적용 완료): `action_status / action_trace / action_result / approved_at / action_live / action_wait / action_inputs`.
- tsx(esbuild)가 `page.evaluate` 함수에 `__name`을 끼워 넣어 깨진다 → evaluate는 **문자열**로 넘긴다.
- Playwright 설치됨(devDependency + chromium-headless-shell). `scripts/agent-brain.ts`가 두뇌, `agent-worker.ts`가 손.

### 음성(03:05) — ElevenLabs

`/api/speech`(가구 인증 + 300자 + 레이트리밋 + 캐시) → `eleven_multilingual_v2`, 남 **Yohan Koo** 기본 · 여 Jiyoung 선택(결과 화면 토글).
실패 시 기기 TTS 폴백. `<audio>`와 speechSynthesis 둘 다 **'사진 찍기' 제스처 안에서 prime**. 키는 `.env`·Vercel 3환경.
PAYG 계정, 문장당 약 80크레딧.

### 디자인(03:00) — 고지서 머리띠 체계

크림 배경 폐기 → 순백. 모든 화면이 남색 머리띠(`AppBar`)로 시작, `mismatch`면 띠가 빨강 + "잠깐만요".
결과는 항목|값 표. 보호자 카드는 3단(판정·핵심값·D-day / **해야 할 일**(`lib/todo.ts`) / 근거 접힘). 홈은 역할 선택(시연 편의, 사용자 결정).
`public/samples/nhis-top.jpg` = 합성 고지서 상단 크롭(계좌 영역 제외).

### 계정 구조(02:50) — 보호자 가입 + 어르신 초대 링크

보호자는 이메일+비밀번호로 가입 → 가구 + 어르신 초대 링크(`/elder?h=토큰`) 생성.
어르신은 링크를 한 번 열면 폰에 저장(주소창에선 지워짐), 이후 가입 없이 자동 싱크.
문서는 가구 단위 격리(남의 것은 404). 암구호 방식은 폐기.
**시연 순서**: 보호자 폰에서 가입 → "링크 보내기" → 어르신 폰에서 링크 열기 → 촬영.

### Codex 2차 리뷰(02:00) 반영 — 배포된 API 가 인증 없이 우편물 원문을 뿌리고 있었다

자녀 화면·목록·PATCH 에 인증을 붙였고, 어르신이 받는 응답에서 원문 필드를 뺐다.
공백으로 전화번호 우회가 다시 열린 것도 막았다. 시험 279개. 상세는 커밋 `git log -1`.
**미반영**: 원자적 quota 예약 RPC, RLS 활성화 — 데모 범위 밖으로 미룸.

### UI 리디자인(02:20) — "AI 티" 제거

9개 디자인 스킬(564K~93K 설치)을 정독·적대 검토한 결정서는 커밋 `design:` 메시지에 요약.
핵심: KoddiUD 온고딕(숫자 고정폭 — 기존 서체는 tnum 이 없어 48px 금액 자릿수가 13px 어긋났다) ·
AAA 7:1 토큰 · Phosphor 인라인 SVG 13개 · 반경 4단 · 모션은 결과 진입 180ms 1회뿐.
**하지 않은 것**: 다크모드·글래스·배경 노이즈·스태거·tracking-tight·캐러셀·Google Fonts.

시연 전 3분 게이트 (전부 0):
```
grep -rnP "[\x{1F300}-\x{1FAFF}]" app components
grep -rnE "rounded-(lg|xl|2xl|3xl)\b|text-(xs|sm|base)\b|animate-spin|active:scale" app components
grep -rnE "neutral-[0-9]|amber-[0-9]|bg-\[#" app components
```
물리 체크: 무음 스위치 해제 · 핀치 줌 + 더블탭 확대 동작 · **시연 직전 페이지 1회 로드해 폰트 캐시**
(font-display: swap — 콜드 로드는 첫 화면이 폴백으로 뜬다) · 200% 확대로 결과 화면 1회 통과.

### 코드로 못 하는 것 — 사람이 해야 한다

1. **아이폰 실기기 확인** (Task 6 Step 5) — `/elder` 에서 사진 찍기 → 결과 + 음성.
   **벨소리 스위치(무음)를 반드시 해제한다.** 첫 발화 잠금은 코드로 풀어놨다(`primeSpeech`).
2. **닫힌 루프 3대 확인** — 노트북(워커 + 프로젝터) · 보호자 폰 `/guardian` · 어르신 폰 `/elder`.
   보호자 [납부 처리 승인] → 노트북 브라우저가 움직임 → 보호자 폰 실시간 화면 → 키패드에서 "보호자 차례" → 폰에서 터치 →
   [이어서 하기] → 어르신 폰 "처리됐어요 — 납부 완료". 로컬 E2E는 전부 확인됨, **실기기·배포 조합은 미확인**.
   어르신 음성(ElevenLabs)이 실기기에서 나오는지도 여기서 본다.
3. **실제 인쇄물 촬영** — 아래 '아직 안 한 것' 참조. 지금까지 전부 화면 이미지·소프트웨어 열화다.
4. **제출 폼 Video · Presentation** — 아직 없다. README 에 에이전트 실행 방법도 아직 없다.
5. (선택) 공공데이터포털 승인 나면 Task 9 Step 8 실시간 조회로 승격.

### 화면에서 바로 보여줄 수 있는 것

~~`/elder` 하단 체험 버튼 3개~~ — **코드에 없다**(07:40 확인). 실물 없이 보여주려면 `fixtures/photo_mid/*.jpg`를 폰 사진첩에 넣고 "사진 찍기"에서 고른다. 호출 1건당 약 $0.04.
시연 계정: `/guardian`에 `shot-test@example.com` / `shotshot12` (개발 DB에 생성된 테스트 계정, 문서 6건 쌓임 — 시연용으로 새 계정 권장).

### 계정 작업 상태

1. ~~**Vercel 로그인**~~ ✅ `ybgwon96` 로그인됨 · 팀 `needslab`에 배포
2. **Supabase 프로젝트 생성** — `ybgwon96's Org`에 새로 만들기로 결정(Task 5에서 CLI로 생성). 이 org는 기존 3개가 전부 일시정지 상태라 free tier 한도에 걸릴 수 있다
3. (선택) **공공데이터포털 활용신청** — Task 9 Step 8의 실시간 조회용. 승인 전에는 스냅샷으로 돌아간다

## 이미 확보된 자산

```
.env                     UPSTAGE_API_KEY (up_xxx) + UPSTAGE_AGENT_ID + SUPABASE_* + GUARDIAN_PASSPHRASE  ※ gitignore됨
registry/                issuer_registry.json — 8개 기관, 출처·확인일 기록 (8/23 부산상수도·부산지법·서울아리수 추가)
data/                    (Task 2에서 registry를 여기로 복사)
fixtures/team/           팀 제작 12장 원본 PNG
fixtures/photo_mid/      촬영 시뮬레이션 mid (12/12 통과) — 샘플 체험용으로 씀
fixtures/photo_hard/     촬영 시뮬레이션 hard (11/12)
scripts/verify.py        검증 계층 원본 (TS 포팅의 기준)
scripts/run_agent.py     Upstage 호출 + raw 덤프
scripts/security_test.py 공격·결손 입력 19종 (19/19 통과)
scripts/simulate_photo.py 촬영 조건 합성
docs/evidence/           raw 응답 JSON + 측정 문서 4건
```

## Upstage 상태

- 에이전트 `agt_PNZaixnk4TZxkDg5muEJQi`, **config #4 저장됨**
- 구성: `Parse → Classify(pay/apply/info/ad) → Extract 분기 2개(Extract-1 납부 9필드 / 추출-2 신청 7필드)`
- **초안(draft)은 API에서 안 보인다.** Studio에서 편집하면 반드시 `설정 #N 초안 ▾ → 저장`
- **미설정 노드가 캔버스에 있으면 저장이 거부된다** ("유효하지 않은 설정"). 규칙 없는 Validate 노드가 그랬다
- 크레딧 $10 중 일부 사용. 문서 1장 ≈ $0.04

## 실측 결과 요약

> ⚠️ **2026-08-23 01:00 갱신.** Codex 리뷰가 검증 계층의 우회 경로 12건을 실제 실행으로 재현했고
> **전부 `scripts/verify.py` 원본에도 있었다.** 아래 "19/19 차단"은 그 벡터들을 덮지 않았다.
> `lib/verify.ts`(TS)에서 전부 막았고 회귀 시험 74종을 추가했다(총 211).
> **Python 원본은 고치지 않았다** — 그때 무엇을 측정했는지의 기록으로 남긴다.

| 조건 | 결과 |
|---|---|
| 팀 fixture 12장 원본 | 12/12 기대 일치 |
| 촬영 시뮬레이션 mid | **11/12** (08번 `clear`→`review`. 아래 참조) |
| 촬영 시뮬레이션 hard | 11/12 (08번이 `clear`→`review`. R3 게이트가 발동한 것으로 **설계대로**) |
| 공격·결손 입력 (Python 19종) | 19/19 |
| 공격·결손 입력 (TS 강화 후) | **211종 통과** — 예금주 부분문자열·번호 이어붙이기·가짜 지사명·ReDoS·분류 계약 포함 |
| 처리시간 | 4.1~26.2초 (n=12 관측 범위. **P95가 아니다**) |

**mid/08 `기초연금_신청안내`가 `clear`→`review`로 바뀐 이유**: 기관명이
`국민연금공단 포항지사 복지지원팀`인데 레지스트리에는 `국민연금공단`만 있다.
구조 매칭(`alias + 하위조직 꼬리`)은 **진짜 지사와 `국민건강보험공단가짜환급센터`를 구분하지 못한다.**
그래서 정확 일치가 아니면 `clear`로 올리지 않는다. 발표에서 그대로 말할 수 있는 트레이드오프다.
되돌리려면 검증된 지사명을 레지스트리에 완전한 alias로 추가하면 된다(출처 확인 필요).

## 제출 폼 남은 칸

- ✅ Demo URL · ✅ Source Code
- ⬜ **Video** · ⬜ **Presentation** — 아직 없다. Description 도 좁혀서 다시 써야 한다.

## 아직 안 한 것 — 오전에 반드시

**실제 인쇄물을 폰으로 찍어 테스트한다.** 지금까지 전부 화면 이미지이거나 소프트웨어 열화다.
종이 질감·손그림자·형광등 모아레·초점 실패는 재현하지 못했다.
`~/Desktop/DOCDO-우편물/00_인쇄용_합본_12장.pdf`를 A4 100% 컬러로 인쇄하면 된다(같은 폴더의 사용법 참고).

## 발표에서 절대 하면 안 되는 말

- ❌ "정확도 100%" / "사칭 탐지율 100%" → ✅ "합성 시나리오 12개에서 분기와 규칙 연결이 재현된 회귀 결과"
- ❌ "P95 26초" → ✅ "n=12 관측 범위 4~26초"
- ❌ `clear`를 "정상"·"진짜"·"안전"으로 표기 → ✅ **"확인된 불일치 없음"**
- ❌ Validate/Merge를 활성 파이프라인으로 그리기 → 우리 구성에 없다. "존재를 확인한 노드"일 뿐
- ❌ "기존 서비스는 전부 push 모델" / "국내외 선례 없음" → 반례 하나에 무너진다
- ❌ "광고 → 버림" → 설계 원칙 4와 충돌. "추출 생략, 자녀 목록 유지"

**공식 연락처를 그대로 복사한 사칭 문서는 우리가 탐지하지 못한다.** 이걸 먼저 밝힌다.

## Q&A 대비 (청중이 Upstage 엔지니어다)

> **"그거 니들이 만든 문서잖아요"**
> "맞습니다. 12장은 저희가 만든 합성 문서이고 모델 성능 평가가 아닙니다. 두 필드만 바꿨을 때 규칙 결과가 달라지는 **통제 실험**입니다. 실문서 일반화 성능은 아직 측정하지 않았습니다."

그 외 예상 질문과 답은 설계서 §10.3에 있다.

## 세션에만 있던 것 (사라지는 것)

- **Codex 리뷰 원문 2건** — `/private/tmp/claude-501/-Users-ybg-projects--sandbox-jungtionx-korea/fe81ab51-f46d-440b-9989-b875450ae653/scratchpad/{spec-review-out.txt,review2-out.txt}`
  지적사항은 전부 코드와 문서에 반영·커밋됐으므로 다시 읽을 필요는 없다.
- **playwriter 세션 15** — Studio·hackjunction 탭. 새 세션에서 `playwriter session new`로 다시 만들면 된다.
  브라우저 로그인 상태는 유지된다.

## 작업 방식 (사용자 지시)

- **팀원에게 의존하지 않는다.** 백엔드·프론트·UI/UX 전부 우리가 만든다.
- **항상 Codex 리뷰를 받으며 진행한다.** `codex exec --skip-git-repo-check - < prompt.md`
  (10분 넘으면 `run_in_background: true`로 돌린다)
- 설계서 §13의 팀원 백엔드 평가 기준(H1~H12)은 이제 적용 대상이 없다.
