# 이어받기 메모 — 2026-08-23 03:00 KST

`/clear` 이후 이 파일부터 읽는다. 대화에만 있고 파일에 없던 것들을 여기 남긴다.

## 지금 상황 한 줄 (2026-08-23 01:45 갱신)

**구현 계획 9개 태스크 전부 완료. 배포·소스·DB 다 살아 있다. 남은 건 영상·발표자료와 실기기 확인.**

## 마감 (오늘)

| 시각 | 일정 |
|---|---|
| **12:00** | **제출 마감** — 11:00에 제출을 시작한다 |
| 13:00~16:00 | 발표 — 팀당 **5분 + Q&A 2분**, 24팀, **청중이 Upstage 팀** |

제출 폼(`hackjunction.app/participate/44/project`)에 채울 것: **Demo URL · Source Code · Video · Presentation**. Description은 이미 제출됐고 좁혀서 다시 써야 한다.

## 확보된 제출물

| 항목 | 값 |
|---|---|
| **Demo URL** | **https://docdo.vercel.app** (공개 확인 — 인증 벽 없음) |
| **Source Code** | **https://github.com/needsbuilder/docdo** (public, 비로그인 clone 검증 완료) |
| Vercel | 팀 `needslab` / 프로젝트 `docdo` · 별칭 `docdo-needslab.vercel.app` |
| 환경변수 | `UPSTAGE_API_KEY`·`UPSTAGE_AGENT_ID`·`SUPABASE_URL`·`SUPABASE_ANON_KEY` → production·preview·development |
| Supabase | 프로젝트 `docdo` ref `tsucihkkdxqzrplanpki` (ap-northeast-2) · `documents` 테이블 적용 완료 · DB 비밀번호는 `.env` |
| **자녀 화면 암구호** | `GUARDIAN_PASSPHRASE` — 값은 `.env` 에. Vercel 3개 환경 등록됨. **시연 전에 자녀 폰에서 한 번 입력해 둔다**(쿠키 12시간) |

## 다음에 할 일

**구현 계획 Task 1~9 전부 완료.** 시험 237개 통과, 빌드·lint 통과, 배포 E2E 확인.

### Codex 2차 리뷰(02:00) 반영 — 배포된 API 가 인증 없이 우편물 원문을 뿌리고 있었다

자녀 화면·목록·PATCH 에 암구호 인증을 붙였고, 어르신이 받는 응답에서 원문 필드를 뺐다.
공백으로 전화번호 우회가 다시 열린 것도 막았다. 시험 279개. 상세는 커밋 `git log -1`.
**미반영**: 원자적 quota 예약 RPC, RLS 활성화 — 데모 범위 밖으로 미룸.

### 코드로 못 하는 것 — 사람이 해야 한다

1. **아이폰 실기기 확인** (Task 6 Step 5) — `/elder` 에서 사진 찍기 → 결과 + 음성.
   **벨소리 스위치(무음)를 반드시 해제한다.** 첫 발화 잠금은 코드로 풀어놨다(`primeSpeech`).
2. **닫힌 루프 2대 확인** (Task 7 Step 3) — 노트북 `/guardian` + 폰 `/elder`.
   자녀가 [처리 완료] → 어르신 화면이 "자녀분이 처리했어요"로 바뀌는지. API 수준은 확인됨.
3. **실제 인쇄물 촬영** — 아래 '아직 안 한 것' 참조. 지금까지 전부 화면 이미지·소프트웨어 열화다.
4. **제출 폼 Video · Presentation** — 아직 없다.
5. (선택) 공공데이터포털 승인 나면 Task 9 Step 8 실시간 조회로 승격.

### 화면에서 바로 보여줄 수 있는 것

`/elder` 하단에 **체험 버튼 3개**(정상 고지서 · 사칭 의심 · 복지 안내문).
심사위원이 실물 없이 실제 파이프라인을 돌려볼 수 있다. 호출 1건당 약 $0.04.

### 계정 작업 상태

1. ~~**Vercel 로그인**~~ ✅ `ybgwon96` 로그인됨 · 팀 `needslab`에 배포
2. **Supabase 프로젝트 생성** — `ybgwon96's Org`에 새로 만들기로 결정(Task 5에서 CLI로 생성). 이 org는 기존 3개가 전부 일시정지 상태라 free tier 한도에 걸릴 수 있다
3. (선택) **공공데이터포털 활용신청** — Task 9 Step 8의 실시간 조회용. 승인 전에는 스냅샷으로 돌아간다

## 이미 확보된 자산

```
.env                     UPSTAGE_API_KEY (up_xxx) + UPSTAGE_AGENT_ID + SUPABASE_* + GUARDIAN_PASSPHRASE  ※ gitignore됨
registry/                issuer_registry.json — 5개 기관, 출처·확인일 기록
data/                    (Task 2에서 registry를 여기로 복사)
fixtures/team/           팀 제작 12장 원본 PNG
fixtures/photo_mid/      촬영 시뮬레이션 mid (12/12 통과) — 샘플 체험용으로 씀
fixtures/photo_hard/     촬영 시뮬레이션 hard (11/12)
scripts/verify.py        검증 계층 원본 (TS 포팅의 기준)
scripts/run_agent.py     Upstage 호출 + raw 덤프
scripts/security_test.py 공격·결손 입력 19종 (19/19 통과)
scripts/simulate_photo.py 촬영 조건 합성
docs/evidence/           raw 응답 JSON + 측정 문서 4건
```

## Upstage 상태

- 에이전트 `agt_PNZaixnk4TZxkDg5muEJQi`, **config #4 저장됨**
- 구성: `Parse → Classify(pay/apply/info/ad) → Extract 분기 2개(Extract-1 납부 9필드 / 추출-2 신청 7필드)`
- **초안(draft)은 API에서 안 보인다.** Studio에서 편집하면 반드시 `설정 #N 초안 ▾ → 저장`
- **미설정 노드가 캔버스에 있으면 저장이 거부된다** ("유효하지 않은 설정"). 규칙 없는 Validate 노드가 그랬다
- 크레딧 $10 중 일부 사용. 문서 1장 ≈ $0.04

## 실측 결과 요약

> ⚠️ **2026-08-23 01:00 갱신.** Codex 리뷰가 검증 계층의 우회 경로 12건을 실제 실행으로 재현했고
> **전부 `scripts/verify.py` 원본에도 있었다.** 아래 "19/19 차단"은 그 벡터들을 덮지 않았다.
> `lib/verify.ts`(TS)에서 전부 막았고 회귀 시험 74종을 추가했다(총 211).
> **Python 원본은 고치지 않았다** — 그때 무엇을 측정했는지의 기록으로 남긴다.

| 조건 | 결과 |
|---|---|
| 팀 fixture 12장 원본 | 12/12 기대 일치 |
| 촬영 시뮬레이션 mid | **11/12** (08번 `clear`→`review`. 아래 참조) |
| 촬영 시뮬레이션 hard | 11/12 (08번이 `clear`→`review`. R3 게이트가 발동한 것으로 **설계대로**) |
| 공격·결손 입력 (Python 19종) | 19/19 |
| 공격·결손 입력 (TS 강화 후) | **211종 통과** — 예금주 부분문자열·번호 이어붙이기·가짜 지사명·ReDoS·분류 계약 포함 |
| 처리시간 | 4.1~26.2초 (n=12 관측 범위. **P95가 아니다**) |

**mid/08 `기초연금_신청안내`가 `clear`→`review`로 바뀐 이유**: 기관명이
`국민연금공단 포항지사 복지지원팀`인데 레지스트리에는 `국민연금공단`만 있다.
구조 매칭(`alias + 하위조직 꼬리`)은 **진짜 지사와 `국민건강보험공단가짜환급센터`를 구분하지 못한다.**
그래서 정확 일치가 아니면 `clear`로 올리지 않는다. 발표에서 그대로 말할 수 있는 트레이드오프다.
되돌리려면 검증된 지사명을 레지스트리에 완전한 alias로 추가하면 된다(출처 확인 필요).

## 제출 폼 남은 칸

- ✅ Demo URL · ✅ Source Code
- ⬜ **Video** · ⬜ **Presentation** — 아직 없다. Description 도 좁혀서 다시 써야 한다.

## 아직 안 한 것 — 오전에 반드시

**실제 인쇄물을 폰으로 찍어 테스트한다.** 지금까지 전부 화면 이미지이거나 소프트웨어 열화다.
종이 질감·손그림자·형광등 모아레·초점 실패는 재현하지 못했다.
`~/Desktop/DOCDO-우편물/00_인쇄용_합본_12장.pdf`를 A4 100% 컬러로 인쇄하면 된다(같은 폴더의 사용법 참고).

## 발표에서 절대 하면 안 되는 말

- ❌ "정확도 100%" / "사칭 탐지율 100%" → ✅ "합성 시나리오 12개에서 분기와 규칙 연결이 재현된 회귀 결과"
- ❌ "P95 26초" → ✅ "n=12 관측 범위 4~26초"
- ❌ `clear`를 "정상"·"진짜"·"안전"으로 표기 → ✅ **"확인된 불일치 없음"**
- ❌ Validate/Merge를 활성 파이프라인으로 그리기 → 우리 구성에 없다. "존재를 확인한 노드"일 뿐
- ❌ "기존 서비스는 전부 push 모델" / "국내외 선례 없음" → 반례 하나에 무너진다
- ❌ "광고 → 버림" → 설계 원칙 4와 충돌. "추출 생략, 자녀 목록 유지"

**공식 연락처를 그대로 복사한 사칭 문서는 우리가 탐지하지 못한다.** 이걸 먼저 밝힌다.

## Q&A 대비 (청중이 Upstage 엔지니어다)

> **"그거 니들이 만든 문서잖아요"**
> "맞습니다. 12장은 저희가 만든 합성 문서이고 모델 성능 평가가 아닙니다. 두 필드만 바꿨을 때 규칙 결과가 달라지는 **통제 실험**입니다. 실문서 일반화 성능은 아직 측정하지 않았습니다."

그 외 예상 질문과 답은 설계서 §10.3에 있다.

## 세션에만 있던 것 (사라지는 것)

- **Codex 리뷰 원문 2건** — `/private/tmp/claude-501/-Users-ybg-projects--sandbox-jungtionx-korea/fe81ab51-f46d-440b-9989-b875450ae653/scratchpad/{spec-review-out.txt,review2-out.txt}`
  지적사항은 전부 코드와 문서에 반영·커밋됐으므로 다시 읽을 필요는 없다.
- **playwriter 세션 15** — Studio·hackjunction 탭. 새 세션에서 `playwriter session new`로 다시 만들면 된다.
  브라우저 로그인 상태는 유지된다.

## 작업 방식 (사용자 지시)

- **팀원에게 의존하지 않는다.** 백엔드·프론트·UI/UX 전부 우리가 만든다.
- **항상 Codex 리뷰를 받으며 진행한다.** `codex exec --skip-git-repo-check - < prompt.md`
  (10분 넘으면 `run_in_background: true`로 돌린다)
- 설계서 §13의 팀원 백엔드 평가 기준(H1~H12)은 이제 적용 대상이 없다.
