# 팀 fixture 12장 E2E — 2026-08-23 01:20 KST

출처: `~/Desktop/DOCDO-우편물/미리보기(앱 화면과 동일)/*.png` (팀 제작, 합성 견본)
파이프라인: `/v2/files → agt_PNZaixnk4TZxkDg5muEJQi (config #3) → verify.py`

| # | 문서 | Classify | Extract 노드 | 판정 | 대조 | 기대 |
|---|---|---|---|---|---|---|
| 01 | 건강보험료 정상 | pay 0.99 | Extract-1 | **clear** | 2/2 | ✅ 초록 |
| 02 | **건강보험료 사칭본** | pay 0.99 | Extract-1 | **mismatch** | **0/2** | ✅ **빨강** |
| 03 | 지방세 체납독촉장 | pay 1.0 | Extract-1 | clear | 2/2 | ✅ |
| 04 | 에너지바우처 신청 | apply 0.99 | 추출-2 | review | 1/2 | ✅ |
| 05 | 전기요금 청구서 | pay 1.0 | Extract-1 | clear | 3/3 | ✅ |
| 06 | 통신요금 미납 | pay 0.99 | Extract-1 | unknown_issuer | 1/1 | ✅ **회색** |
| 07 | 주민세 고지서 | pay 1.0 | Extract-1 | clear | 2/2 | ✅ |
| 08 | 기초연금 신청안내 | apply 0.99 | 추출-2 | clear | 2/2 | ✅ |
| 09 | 보험갱신 DM | ad 0.99 | — | no_extract | — | ✅ 버림 |
| 10 | 마트전단 | ad 1.0 | — | no_extract | — | ✅ 버림 |
| 11 | 상조DM | ad 1.0 | — | no_extract | — | ✅ 버림 |
| 12 | 손글씨메모 | info **0.18 low** | — | **needs_human** | — | ✅ 검토함 |

**12/12 기대와 일치.** 처리시간 9.6~26.2s (평균 ~16s, 우리 합성 fixture보다 느림 — 실제 양식이 복잡).

## 이 세트가 드러낸 설계 결함 3가지 (전부 수정함)

### ① R1 보류 규칙이 정답을 버렸다 — 치명적
실제 고지서에는 **홈페이지 URL이 없다**. `info_url`이 빈 값 + `low`로 나오는데,
"대조 대상이 low면 R1 보류"라는 규칙 때문에 **전화번호 대조까지 통째로 건너뛰었다.**
→ 01·02·08이 전부 `review`로 빠지고 **사칭본을 놓쳤다.**

정작 핵심 신호는 완벽히 뽑혀 있었다:
- 01 `contact_phone: 1577-1000` [high]
- 02 `contact_phone: 010-4821-7733` [high]

**수정: 필드별 독립 판정.** 한 필드가 비어도 다른 필드 대조는 계속한다.

### ② 진짜 관공서 문서를 사칭으로 오판했다 — 더 치명적
03·07(진짜 포항시 고지서)의 `054-270-6230`(세무과 직통)이
레지스트리의 `054-270-8282`(대표번호)와 다르다는 이유로 **mismatch** 판정.

**기관은 대표번호 하나만 쓰지 않는다.** 부서 직통번호가 있다.
진짜 지방세 고지서에 "사칭 의심"이 뜨면 제품이 못 쓰게 된다.

**수정 2가지:**
- `phone_prefixes` 국번 대역 매칭 — `054-270-*`는 같은 기관으로 인정
- **판정 강도 재조정**: 전화번호 단독 불일치는 `review`(노랑)까지만.
  `mismatch`(빨강)는 **개인 휴대전화(R4)** 또는 **도메인 불일치**처럼 확정 신호만.

### ③ 발급기관을 너무 세부적으로 추출한다
`국민연금공단 포항지사 복지지원팀`, `포항시 북구 행정복지센터 두호동 맞춤형복지팀`
→ 레지스트리 매칭 실패.
**수정: `strip_org()`** — 지사·센터·팀·과 등 조직 접미사에서 잘라 최상위 기관명만 사용.

## 추가된 규칙

| 규칙 | 내용 | 근거 |
|---|---|---|
| **R4** | 고지서 상담번호가 **개인 휴대전화(010/011/016~019)**면 위험 | 기관은 대표번호를 쓴다. **레지스트리 등록 여부와 무관하게 작동** → 커버리지 공백을 메운다 |
| **R5** | **Classify confidence가 `low`**면 분류 자체를 믿지 않고 사람에게 | 12번 손글씨메모(0.18)를 정확히 잡아냄 |

R4가 특히 중요하다. 02 사칭본에 **신호 2개**(R1 불일치 + R4 개인폰)가 동시에 잡힌다.

## 남은 한계 (발표에서 밝힐 것)
- 레지스트리 5개 기관뿐. **민간 사업자(KT 등)는 대조 대상이 아니다** → `unknown_issuer`
- 공식 대표번호를 그대로 복사한 사칭 문서는 R1으로 못 잡는다 (R4로도 못 잡음)
- n=12, 전부 합성 견본. **실촬영(구긴 종이·기울임·조명) 테스트는 아직**
