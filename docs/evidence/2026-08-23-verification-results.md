# 검증 계층 E2E 결과 — 2026-08-23 00:40 KST

파이프라인: `iPhone 촬영용 fixture → /v2/files → Studio 에이전트(config #3) → verify.py`
에이전트 `agt_PNZaixnk4TZxkDg5muEJQi` · config #3 (Parse → Classify 4종 → Extract 분기 2개)

| # | fixture | Classify | Extract 노드 | verdict | 대조 |
|---|---|---|---|---|---|
| ① | 통제본 | pay (high 1.0) | `Extract-1` | **clear** 확인된 불일치 없음 | 2/2 일치 |
| ② | **변조본** | pay (high 1.0) | `Extract-1` | **mismatch** 공식 정보와 다른 항목 발견 | **0/2** |
| ③ | 복지안내 | apply (high 0.99) | `추출-2` | **clear** | 2/2 일치 |
| ④ | 광고 | ad (high 1.0) | 없음(미매핑) | no_extract | — |

## ①②의 대비 — 발표 핵심 장면

같은 문서에서 **두 필드만** 바꿨다.

| 필드 | ① 통제본 | ② 변조본 | 판정 |
|---|---|---|---|
| 문의전화 | 1577-1000 | **1588-9876** | ! 공식 `1577-1000`과 불일치 |
| 안내 주소 | nhis.or.kr | **nhis-pay.co.kr** | ! 공식 `nhis.or.kr`과 불일치 |
| 금액·기한·EPN·기관 | 동일 | 동일 | Extract는 둘 다 정확히 읽음 |

**Extract는 변조된 값도 정확히 읽어냈다.** 판정을 가른 건 레지스트리 대조다.
= "AI가 가짜를 알아본다"가 아니라 "공식 정보와 대조해 다른 점을 찾는다"는 우리 주장 그대로.

## 화면 표시 형태
```
공식 정보 대조 (검사 2건 중 2건 일치)
  ✓ 문의전화 — 1577-1000
  ✓ 안내 주소 — nhis.or.kr
  — 계좌 진위 — 확인하지 않음 (계좌 명의 조회 권한 없음)
```
전역 verdict만 크게 띄우지 않고 **무엇을 몇 개 확인했고 무엇을 안 했는지**를 보여준다.

## 구현된 안전 게이트
- **R3**: 핵심 필드(금액·기한·기관)에 `low`가 있으면 `speech_suppressed=true` → 숫자 낭독 억제
- **R1 보류**: 대조 대상(전화·URL)이 `low`면 R1을 아예 실행하지 않고 `review` — OCR 오독을 공식 불일치로 오판하지 않기 위해
- **not_checkable**: 대조할 연락처가 없으면 `clear`를 주지 않는다
- **unknown_issuer**: 레지스트리 미등록 기관은 판단 불가
- **URL 경계 비교**: `urlparse().hostname` 후 정확 일치. 부분 문자열 검사 금지(`evil.com/go.kr` 차단)

## 저장 실패 원인 (기록)
"유효하지 않은 설정 — 설정을 저장하지 못했어요"의 원인은 **규칙이 하나도 없는 Validate 노드**였다.
미설정 노드가 캔버스에 있으면 config 저장이 거부된다. 제거하니 즉시 저장됨.
→ Validate를 다시 넣을 때는 **규칙을 먼저 채우고** 저장해야 한다.
